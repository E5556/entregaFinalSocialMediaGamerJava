<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.optic.socialmediagamer">

    <uses-feature
        android:name="android.hardware.camera2"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".GamerApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_dragonball"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_dragonball_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.EditProfileActivity"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.optic.socialmediagamer"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.PostActivity" />
        <activity android:name=".activities.PostDetailActivity" />
        <activity android:name=".activities.ChatActivity" />
        <activity android:name=".activities.UserProfileActivity" />
        <activity android:name=".activities.CompleteProfileActivity" />
        <activity
            android:name=".activities.HomeActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".activities.RegisterActivity" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="false" />
        <activity android:name=".activities.SearchPostsActivity" />
        <activity android:name=".activities.CreateStoryActivity" />
        <activity android:name=".activities.ViewStoryActivity" />
        <activity android:name=".activities.CreateLFGActivity" />
        <activity android:name=".activities.CreateEventActivity" />
        <activity android:name=".activities.CreateCommunityActivity" />
        <activity android:name=".activities.CommunityDetailActivity" />
        <activity android:name=".activities.EventDetailActivity" />
        <activity android:name=".activities.GroupChatActivity" />

        <service
            android:name=".services.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>